# Public — safe to expose to the browser (NEXT_PUBLIC_* is bundled client-side)
NEXT_PUBLIC_CONTRACT_ID=CBILWM2EXW7CQPZBZHGZ6OLLZIST44BAXWM6A2GZLDEYZTMK7EWXFVDA
NEXT_PUBLIC_RPC_URL=https://soroban-testnet.stellar.org:443
NEXT_PUBLIC_HORIZON_URL=https://horizon-testnet.stellar.org
NEXT_PUBLIC_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# Server-side only — never commit real values, never prefix with NEXT_PUBLIC_
DATABASE_URL=                    # Neon Postgres connection string
FLUPY_ROOT_OPERATOR_SECRET=      # RootOperator signing key (Stellar secret key, S...)
FLUPY_ALLOW_DEMO_ENROLLMENT=     # 'true' to allow enrollment on a testnet demo deployment
CRON_SECRET=                     # shared secret checked by /api/admin/sync-root
VERCEL_BYPASS_SECRET=            # Protection Bypass for Automation, if the deployment
                                  # sits behind Vercel Deployment Protection
