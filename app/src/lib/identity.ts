'use client';

export {
  credentialExists,
  createCredential,
  deleteCredential,
  generateSecret,
  unlockCredential,
} from '@fluppy/browser';

export type {
  CreateCredentialResult,
} from '@fluppy/browser';
