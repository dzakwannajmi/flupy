'use client';

export {
  credentialExists,
  createCredential,
  deleteCredential,
  generateSecret,
  unlockCredential,
} from '@flupy/browser';

export type {
  CreateCredentialResult,
} from '@flupy/browser';
